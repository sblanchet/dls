
$Id$

* dlsd:
    - Automatic restart on error
    - Channel trigger 
    - Restart delay as command-line option

* dls:
    - option -Q for really quiet
    - export:
        * downsample (time, value, min, max)
        * start/end time in epoch
        * additional information in dls_export_info (channel name, unit,
            command-line options, intersected chunks)
        * dls_export_info as XML
	- split
	- delete
	- check
	- repair

* lib:
	- fetch_chunks(): only update

* dls_view:
  - add shortcuts
  - current time button 
  - auto-Scrolling?
  - graphical error messages at export
  - Qt

* dls_ctl
  - sort channel list
  - search function in channel list
  - Qt
